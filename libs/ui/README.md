# UI Library

Standalone Angular UI components powered by the design tokens. Import the components you need and include the tokens + base styles globally.

## Usage

```ts
import { DsButtonDirective, DsCardComponent } from '@ds-angular/ui';
```

All components are standalone, so include them in the `imports` array of your standalone component.
